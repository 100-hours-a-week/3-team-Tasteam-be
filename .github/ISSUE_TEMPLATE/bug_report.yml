name: Bug Report
description: 재현 가능한 버그 제보
title: "[BE] "
labels:
  - 버그-심각
assignees: []

body:
  - type: markdown
    attributes:
      value: |
        ---
  - type: markdown
    attributes:
      value: |
        ## 1. 🐞 버그 요약 (필수)
  - type: textarea
    id: summary
    attributes:
      label: 버그 요약
      description: 무엇이 잘못됐는지 한 문장으로 정리하되 실제로 본 증상/화면을 포함
      placeholder: |
        예: 결제 완료 후 주문 내역이 비어 있어서 고객이 결제가 되었는지 확인할 수 없음
        예: 주문 생성 시 [XYZ-1001] 에러가 발생하면서 프론트에서 아무 반응 없음
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
  - type: markdown
    attributes:
      value: |
        ## 2. 🌍 버그 발생 위치 (필수)
  - type: input
    id: location
    attributes:
      label: 발생 위치
      description: 어느 서비스/모듈의 어느 엔드포인트나 화면에서 문제가 생겼는지, 가능한 경로까지
      placeholder: |
        예: app-api/src/main/java/com/tasteam/order/OrderService.java#completeOrder
        예: 프론트 `/checkout/confirm` 버튼 클릭 이후
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
  - type: markdown
    attributes:
      value: |
        ## 3. 💥 에러 코드 및 설명 / 화면 캡처 (필수)
  - type: textarea
    id: error_details
    attributes:
      label: 에러 및 캡처
      description: 서버 로그/클라이언트 에러 코드, 메시지, 예외 스택 + 화면 캡처 링크나 위치(Cloud, Figma 등). 가능하면 json/response 예시도 붙여주세요.
      placeholder: |
        - 에러 코드: XYZ-123
        - 메시지: null pointer at OrderService.completeOrder()
        - 스택/로그: stack trace 붙이기
        - 화면 캡처: https://drive.google.com/...
        - API 응답: `{"error":"..."}`
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
  - type: markdown
    attributes:
      value: |
        ## 4. 🔁 재현 동작 (선택)
  - type: textarea
    id: steps
    attributes:
      label: 재현 절차
      description: 가능하면 1~3 단계로 구체화(환경, 입력값, 누른 버튼 등). 선택 항목입니다.
      placeholder: |
        1. 2026-01-27 prod 앱에서 ...
        2. 폼에 A 입력 후 B 버튼 클릭
        3. 응답이 500으로 떨어짐

  - type: markdown
    attributes:
      value: |
        ---
  - type: markdown
    attributes:
      value: |
        ## 5. ✅ 예상 동작 (선택)
  - type: textarea
    id: expected
    attributes:
      label: 예상 동작
      description: 정상 흐름일 경우 기대하는 화면/응답/처리 내용을 적어주세요. 참고용 그림 링크도 좋습니다.

  - type: markdown
    attributes:
      value: |
        ---
  - type: markdown
    attributes:
      value: |
        ## 6. ⚠️ 참고사항 (선택)
  - type: textarea
    id: notes
    attributes:
      label: 참고사항
      description: 임시 우회, 관련 이슈/PR 링크, 테스트한 환경(브라우저/OS), 배포 채널 등 자유롭게 추가
