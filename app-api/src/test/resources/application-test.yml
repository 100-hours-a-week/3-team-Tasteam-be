spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false # Tests use Hibernate create-drop; enable Flyway only in dedicated migration-verification runs.

  security:
    jwt:
      secret: test-secret-129310298301928309128309182309182308490127403912380912
      accessTokenExpiration: 1800000
      refreshTokenExpiration: 1209600000

tasteam:
  admin:
    username: admin
    password: admin
