#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"
set -euo pipefail

echo "🔁 실행 테스트를 진행합니다..."
if ! ./gradlew test; then
  echo "⛔ 테스트 실패! 푸시가 중단됩니다."
  exit 1
fi

echo "✅ 테스트 통과! 푸시를 계속합니다."
